<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="@color/background_color"> <!-- Cor de fundo personalizada -->

    <!-- Campo de entrada para o nome do Pokémon -->
    <EditText
        android:id="@+id/etPokemonName"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Digite o nome do Pokémon"
        android:inputType="text"
        android:background="@drawable/rounded_edittext"
        android:padding="12dp"
        android:textColorHint="@color/hint_color"
        android:textColor="@color/text_color"
        android:layout_marginBottom="16dp" />

    <!-- Botão para buscar o Pokémon -->
    <Button
        android:id="@+id/btnSearch"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Buscar Pokémon"
        android:textColor="@color/white"
        android:backgroundTint="@color/button_color"
        android:padding="12dp"
        android:textStyle="bold"
        android:elevation="4dp"
        android:layout_marginBottom="16dp" />

    <!-- TextView para mostrar o nome do Pokémon -->
    <TextView
        android:id="@+id/tvPokemonName"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Nome do Pokémon"
        android:textSize="20sp"
        android:padding="16dp"
        android:textColor="@color/title_color"
        android:gravity="center"
        android:textStyle="bold" />

    <!-- ImageView para exibir a imagem do Pokémon -->
    <ImageView
        android:id="@+id/ivPokemonImage"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:layout_gravity="center"
        android:layout_marginTop="16dp"
        android:layout_marginBottom="24dp"
        android:src="@drawable/ic_launcher_background"
        android:background="@drawable/rounded_imageview" /> <!-- Fundo arredondado para imagem -->
</LinearLayout>
